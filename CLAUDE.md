# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**terra** is a team retrospective board application. The repository is currently in its early stages — only a README exists. No source code, build system, or dependencies have been added yet.

## Architecture

The application is structured as a frontend/backend split with a relational database, running in containers.

- **Frontend**: React with TypeScript.
- **Backend**: Python 3 (latest stable version).
- **Database**: PostgreSQL.
- **Containers**: Podman.

## Getting Started

There are no build, lint, or test commands yet. When they are added, update this file with the relevant commands.
